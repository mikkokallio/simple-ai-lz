# Frontend environment variables for production build
# Backend API URL will be injected by Container Apps at runtime

# For local development
VITE_BACKEND_API_URL=http://localhost:8080

# Note: In production, this is set via Container Apps environment variable
